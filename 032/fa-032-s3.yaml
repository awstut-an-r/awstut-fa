AWSTemplateFormatVersion: 2010-09-09

Parameters:
  EIP:
    Type: String

  Prefix:
    Type: String
    
  VPC:
    Type: String
    

Resources:
  Bucket:
    Type: AWS::S3::Bucket
    Properties:
      BucketName: !Ref Prefix
      AccessControl: Private
      #AccessControl: PublicReadWrite
      WebsiteConfiguration:
        IndexDocument: index.html
      
  BucketPolicy:
    Type: AWS::S3::BucketPolicy
    Properties:
      Bucket: !Ref Bucket
      PolicyDocument:
        Version: 2012-10-17
        Statement:
          #- Action:
          #    - s3:*
          #  Effect: Deny
          #  Resource:
          #    #- !Sub "arn:aws:s3:::${Bucket}"
          #    #- !Sub "arn:aws:s3:::${Bucket}/*"
          #    - !GetAtt S3Stack.Outputs.BucketArn
          #    - !Sub
          #        - "${BucketArn}/*"
          #        - BucketArn: !GetAtt S3Stack.Outputs.BucketArn
          #  Condition:
          #    NotIpAddress:
          #      "aws:SourceIp":
          #        - !GetAtt VPCStack1.Outputs.EIP
          #    StringNotEquals:
          #      "aws:SourceVpce":
          #        #- !GetAtt VPCStack1.Outputs.VPC
          #        - !GetAtt VPCStack2.Outputs.VPC
          #  Principal: "*"
          - Action:
              - s3:*
            Effect: Allow
            Resource:
              - !Sub "arn:aws:s3:::${Bucket}"
              - !Sub "arn:aws:s3:::${Bucket}/*"
              #- !GetAtt S3Stack.Outputs.BucketArn
              #- !Sub
              #    - "${BucketArn}/*"
              #    - BucketArn: !GetAtt S3Stack.Outputs.BucketArn
            Condition:
              IpAddress:
                "aws:SourceIp":
                  #- !GetAtt VPCStack1.Outputs.EIP
                  - !Ref EIP
              #StringEquals:
              #  "aws:SourceVpce":
              #    #- !GetAtt VPCStack1.Outputs.VPC
              #    - !GetAtt VPCStack2.Outputs.VPC
            Principal: "*"
          - Action:
              - s3:*
            Effect: Allow
            Resource:
              - !Sub "arn:aws:s3:::${Bucket}"
              - !Sub "arn:aws:s3:::${Bucket}/*"
              #- !GetAtt S3Stack.Outputs.BucketArn
              #- !Sub
              #    - "${BucketArn}/*"
              #    - BucketArn: !GetAtt S3Stack.Outputs.BucketArn
            Condition:
              #IpAddress:
              #  "aws:SourceIp":
              #    - !GetAtt VPCStack1.Outputs.EIP
              StringEquals:
                "aws:SourceVpc":
                  #- !GetAtt VPCStack2.Outputs.VPC
                  - !Ref VPC
            Principal: "*"
          
          
Outputs:
  Bucket:
    Value: !Ref Bucket
    
  BucketWebsiteURL:
    Value: !GetAtt Bucket.WebsiteURL
    
  #BucketArn:
  #  Value: !GetAtt Bucket.Arn

  #BucketWesSiteEndpointUrl:
  #  Value: !Sub "https://s3-ap-northeast-1.amazonaws.com/${S3Bucket}" 